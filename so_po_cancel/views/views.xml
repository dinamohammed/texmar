<odoo>
  <data>
    <!-- explicit list view definition -->

    <!--
    <record model="ir.ui.view" id="so_po_cancel(2).list">
      <field name="name">so_po_cancel(2) list</field>
      <field name="model">so_po_cancel(2).so_po_cancel(2)</field>

      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>

    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="so_po_cancel(2).action_window">
      <field name="name">so_po_cancel(2) window</field>
      <field name="res_model">so_po_cancel(2).so_po_cancel(2)</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="so_po_cancel(2).action_server">
      <field name="name">so_po_cancel(2) server</field>
      <field name="model_id" ref="model_so_po_cancel(2)_so_po_cancel(2)"/>

      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",

          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="so_po_cancel(2)" id="so_po_cancel(2).menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="so_po_cancel(2).menu_1" parent="so_po_cancel(2).menu_root"/>
    <menuitem name="Menu 2" id="so_po_cancel(2).menu_2" parent="so_po_cancel(2).menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="so_po_cancel(2).menu_1_list" parent="so_po_cancel(2).menu_1"
              action="so_po_cancel(2).action_window"/>
    <menuitem name="Server to list" id="so_po_cancel(2)" parent="so_po_cancel(2).menu_2"
              action="so_po_cancel(2).action_server"/>
    -->

  </data>
</odoo>