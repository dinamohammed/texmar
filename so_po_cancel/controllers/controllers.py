# -*- coding: utf-8 -*-

from odoo import http

# class SoPoCancel(2)(http.Controller):
#     @http.route('/so_po_cancel(2)/so_po_cancel(2)/', auth='public')
#     def index(self, **kw):
#         return "Hello, world"

#     @http.route('/so_po_cancel(2)/so_po_cancel(2)/objects/', auth='public')
#     def list(self, **kw):
#         return http.request.render('so_po_cancel(2).listing', {
#             'root': '/so_po_cancel(2)/so_po_cancel(2)',
#             'objects': http.request.env['so_po_cancel(2).so_po_cancel(2)'].search([]),
#         })

#     @http.route('/so_po_cancel(2)/so_po_cancel(2)/objects/<model("so_po_cancel(2).so_po_cancel(2)"):obj>/', auth='public')
#     def object(self, obj, **kw):
#         return http.request.render('so_po_cancel(2).object', {
#             'object': obj
#         })

